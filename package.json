{
  "name": "hiero-enterprise-react",
  "version": "0.1.0",
  "private": true,
  "description": "React / Next.js integration kit for Hiero networks",
  "author": "I-CODERS <i-coders@uttecam.edu.mx>",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/MrpotetoR/Hiero-R.git"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "prettier": "^3.2.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
